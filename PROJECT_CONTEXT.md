# Project Context

- 2026-09-03: Removed the redundant visible `Details URL` column from the Lab 3 table because each show name already links to the same TVmaze page. The CSV and parsed `show_url` field remain unchanged so the name links continue to work. Updated the page instructions to describe 11 displayed columns.
- 2026-09-03: Verified `data/lab3_data.csv` contains exactly 2,000 rows and 2,000 unique show IDs. Its ID range is 1–2084 with 84 gaps, so the largest ID is not the record count; the acquisition script intentionally stops when `DEFAULT_TARGET = 2_000` unique records have been collected.
- 2026-09-03: Split the Lab 3 dataset field description into three visible lines using HTML `<br>` elements, with four fields per line for readability.
- 2026-09-03: Audited missing values in the current 2,000-row Lab 3 CSV. Across the 11 displayed fields there are 831 blank cells in 599 rows: genres 245, premiered 1, runtime_minutes 114, rating 420, and country 51; all other displayed fields are complete. The table renders blanks as `N/A` and keeps them last during sorting, so it appears visually filled. ID gaps are deleted or skipped source identifiers and are separate from missing cell values.
- 2026-09-03: Clarified the Lab 3 count label as `Unique shows loaded` because all 2,000 records are present even when individual fields are N/A. Restored the `quality-summary` element so the existing JavaScript displays per-field missing-value counts separately.
- 2026-09-03: Reworded the Lab 3 data-quality explanation to distinguish omitted TVmaze shows, which create gaps in the ID sequence and are absent from the CSV, from missing fields within the 2,000 included records, which are displayed as N/A.
- 2026-09-03: Confirmed local `main`, `origin/main`, and GitHub all point to commit `708a876`. The live GitHub Pages Lab 3 URL serves the latest wording and returns `Cache-Control: max-age=600`, explaining a possible delay of up to 10 minutes after a push. No Pages workflow runs or commit statuses were reported for this deployment.
- 2026-09-05: Built the Lab 4 submission around the 14,640-row Twitter US Airline Sentiment dataset (Crowdflower/Kaggle, CC BY-NC-SA 4.0). The reproducible pipeline removes duplicate tweet IDs, validates dates and retweet counts, performs TF-IDF preprocessing, recomputes sentiment with CardiffNLP's Twitter RoBERTa model without using the supplied labels, and exports clean and airline-level CSV files. The Lab 4 page uses D3 to compare the percentage distribution of model-estimated sentiment across six airlines and includes a 100-200 word analysis, source/license credit, accessible data table, and data/script links.
- 2026-09-05: Restored the Lab 4 chart's fixed 920px coordinate system with a 760px rendered minimum width on narrow screens. The chart now scrolls horizontally instead of compressing the Negative, Neutral, and Positive segments; its scroll container is keyboard-focusable and retains touch scrolling.
- 2026-09-05: Simplified the Lab 4 dataset summary from a bordered card layout to three compact text rows, and changed the sentiment table from full-width to a compact 520px minimum width so the Airline and Negative columns sit closer together.
- 2026-09-06: Audited Lab 4 against the seven submission checklist items and the course assignment instructions. Fresh CSV checks confirmed 14,640 raw rows, 14,485 cleaned rows, no missing cleaned fields or duplicate tweet IDs, valid sentiment probabilities/labels/scores on every cleaned row, and exact agreement between tweet-level counts and all six airline summaries. The analysis contains 141 whitespace-delimited words and covers the five requested topics. Safari successfully rendered the local chart, summary table, counts, and analysis; all local page asset/data/script links returned HTTP 200, and JavaScript/Python syntax checks passed. Full RoBERTa inference was not rerun because the active Python environment lacks its dependencies. The 50-row lab4_dirty_tweets.csv is unused by the submitted pipeline. Submission remains incomplete: Lab 4 scripts/data are untracked in local Git, and the live GitHub Pages page still says "Lab 4 will be added here."; the live cleaning script, JavaScript, and airline-summary CSV returned HTTP 404. Remaining actions are to include the required files in Git, commit/push them, verify the deployed page and links, and submit the direct Lab 4 Pages URL. The course prefers datasets not primarily built for precomputed sentiment labels; this is a preference, and the current pipeline explicitly recomputes estimates without using those labels. No assignment source or data was changed during this audit.
- 2026-09-06: Moved the unused 50-row example CSV from lab4/lab4_dirty_tweets.csv to data/lab4_dirty_tweets.csv to keep datasets together. Confirmed there are no source-code references requiring changes, and verified the file SHA-256 is unchanged after relocation. The airline-data pipeline still uses data/lab4_raw_tweets.csv.
- 2026-09-06: Rechecked the full live course Lab4.md, including Tasks 1-15, rather than only the final Assignment section. The current pipeline matches the specified RoBERTa model, light normalization of tweet_text_raw, argmax class selection, and P(positive)-P(negative) score; its TF-IDF uses the demonstrated min_df=2 and max_df=0.90. Clarification: it uses TweetTokenizer and scikit-learn English stop words instead of the tutorial word_tokenize and NLTK stopwords, does not separately construct/inspect the CountVectorizer frequency DTM in Tasks 8-9, and does not execute the classroom exercises on the 50-row example CSV. These are differences from completing every tutorial task; the final Assignment does not explicitly require submitting those intermediate demonstrations. The dataset choice also differs from the stated preference for data not primarily built for precomputed sentiment labels. The core assignment implementation is aligned, but a claim that every tutorial step or the complete submission has been finished would be too broad. Full model inference was not rerun in this review; live Pages still serves the placeholder, and required local files remain untracked. No pipeline changes were made.
- 2026-09-06: User clarified the review standard: final deliverables follow the final submission checklist, while implementation methods should follow the relevant tutorial Tasks. Treat absence of a classroom intermediate artifact from the submission list as distinct from permission to substitute methods. Under this standard, the unnecessary TweetTokenizer and sklearn stop-word substitutions should be aligned with Task 7 word_tokenize and NLTK English stopwords; the explicit frequency DTM step should be included when completing the Tasks 8-10 text-analysis workflow. Data-specific field mapping and airline aggregation are appropriate adaptations, and using the 50-row exercise file is not required for the final >=1,000-tweet assignment. This turn clarified the review interpretation only; implementation was not changed.
- 2026-09-06: Implemented the user-approved Task 7-10 alignment in lab4/clean_tweets.py: NLTK word_tokenize, NLTK English stopwords, WordNet lemmatization, explicit normalized/token/stopword/lemma stages, and a separate sparse CountVectorizer DTM with a bounded inspection preview before TfidfVectorizer. Both matrices use min_df=2 and max_df=0.90, and their vocabularies are checked for equality. Required NLTK resources are obtained with explicit download-error handling. Added four hand-calculated regression tests in lab4/test_clean_tweets.py; all passed, and the example distinguishes the prior preprocessing. The full CLI pipeline was rerun successfully, including all 14,485 RoBERTa predictions, using /tmp/stats401-lab4-task-alignment-venv/bin/python with HF_HUB_DISABLE_XET=1 and temporary output /tmp/stats401-lab4-task-alignment-output. Environment: pandas 2.2.3, numpy 2.1.3, nltk 3.9.1, scikit-learn 1.6.1, torch 2.14.0, transformers 4.57.6; model revision 3216a57f2a0d9c45a2e6c20157c20c49fb4bf9c7. DTM and TF-IDF shapes are 14,485 x 4,897. All text_clean rows changed, while every sentiment probability and label exactly matched the prior results. Verified unique IDs, no missing clean fields, probability/score invariants, term document frequencies, all airline summaries, and the analysis claims before installing the four regenerated outputs in data/. Original raw CSV hash remains unchanged. Prior script and generated outputs are preserved under data/archive/lab4/2026-09-06-before-task-alignment/ with a hash manifest. Updated the page analysis to describe the actual NLTK/DTM methods; it is 155 words and renders in Safari. No commit, push, or deployment was performed.
- 2026-09-06: Reviewed the user-written replacement Analysis in lab4/index.html without editing it. It is 153 words; its visual-encoding explanation and the 64.7% US Airways / 38.3% Virgin America observations agree with current outputs. It should add the dataset identity/size, main cleaning decisions, and a clearer explanation of how RoBERTa derives sentiment to cover all five requested analysis topics. Recommend referring to sentiment in sampled airline-related tweets rather than all customer sentiment. Clarified that the assignment asks students to choose a >=1,000-row tweet dataset, that the additional-attribute list gives examples rather than requiring every field, and that airline is the additional category used by this visualization. The user wants to write the analysis personally; provide an outline and feedback rather than replacing their prose.
- 2026-09-06: At the user's explicit request, revised the Lab 4 Analysis in their first-person explanatory style (attribute type followed by a reason for the visual mapping). The three paragraphs cover dataset/cleaning/sentiment methods, visual encodings, and observations/limitations. Verified 178 words and checked the record counts and quoted percentages against current outputs. This authorization supersedes the preceding request for feedback-only assistance on the prose. No data, scripts, chart, or deployment was changed.

- 2026-09-06: At the user's explicit request, committed the 12 Lab 4 source/data/style files and pushed main to origin successfully as 5f06157139d35a0f803086345897f9a6fa61fa79 (Complete Lab 4 tweet cleaning and sentiment visualization). Fresh verification passed all four Python regression tests and the JavaScript syntax check; CSV checks confirmed 14,485 unique cleaned tweets, valid per-tweet sentiment estimates, consistent six-airline summaries, and a 178-word analysis. Live git ls-remote matches local HEAD. GitHub Pages serves the current page, JavaScript, cleaning script, summary CSV, and cleaning report with HTTP 200 and matching local hashes. Safari rendered the deployed six-airline chart, summary table, counts, and current analysis successfully at https://jiaxuan-mao.github.io/stats401-labs/lab4/. Used the existing system HTTP proxy on localhost:7897 for Git after direct connectivity failed, without changing permanent Git settings. Local PROJECT_CONTEXT.md, .DS_Store, and data/archive/ were intentionally excluded from the commit. The course submission itself still requires the user to provide the direct Lab 4 URL.


## 2026-09-10 — Lab 5 explanation and implementation pending source data

- User requested explanation first, then help completing Lab 5, from `/Users/mable/Downloads/Lab5.md`.
- Confirmed actual course repository remains `/Users/mable/Desktop/Academy/stats 401/Lab`; no README or JS package manifest exists. Read source files and preserved other labs and raw data.
- Assignment Part A has stale generic field names. Use district/daily_passengers/station_type and travel_time_min/route_type, as specified in the transit dataset description and final checklist.
- Created `lab5/lab5.js`, replaced the existing Lab 5 HTML placeholder, and appended only `.lab5-page` scoped CSS. Uses external CSV loading, validation, D3 force layout with seeded random source 401, node area/color/shape, route width/dash pattern, draggable nodes, hover/focus/tap inspection, linked highlighting, selection/reset, reduced-motion pause, and a symmetric district-ordered matrix with route hue/time opacity. Original ID-based routes are kept separate from forceLink's mutated copy.
- Original `lab5_assignment_stations.csv` and `lab5_assignment_routes.csv` were NOT found in Downloads, the actual course project, or the Documents workspaces. Asked user for file paths or the course download link. Do not generate replacement assignment data or infer observations from the two-row examples.
- IMPORTANT: Lab 5 is NOT complete or ready to submit. Six-question table contains explicit pending observations. After authentic CSVs arrive: inspect shape/types/missing/duplicates/endpoints and any unsupported parallel edges; inspect real rendered diagrams; write all six actual observations; keep C1+C2+C3 about 150–300 English words; verify final page. No Lab 5 commit, push, or deployment in this turn.
- Chinese Task 1–16 explanation and assignment guide: `/Users/mable/Documents/ChatGPT/stats 401/output/notes/Lab5_讲解与作业思路.md`. Includes execution flow, endpoint mutation, area versus radius, ordinal versus nominal attributes, drag lifecycle, matrix symmetry, six-question observation plan, and six practice questions. D3 API details checked against official link/simulation/symbol documentation.
- Verification: `node --check lab5/lab5.js` and `git diff --check` passed. Playwright using installed Chrome tested the real missing-data state and intercepted temporary 50-node/50-route fixtures (not stored in data/). Verified 50 node marks, 50 routes, 2,500 matrix cells, exactly 100 connected cells, symmetry, visual channels, station selection/degree, keyboard tooltips, linked highlights, reordering, real pointer drag with fx/fy released, motion controls, 390px local scrolling without page overflow, and six malformed-data rejections. Zero uncaught JS errors. Inspected network/matrix/mobile screenshots. Results and fixtures are QA only and prove no actual assignment findings.
- QA script and screenshots: Documents workspace `tmp/lab5_qa.cjs` and `tmp/lab5-qa/`. Local HTTP server on `http://127.0.0.1:8015/lab5/` serves the actual Desktop repository (session 44258); retained for preview, but missing-data state is expected until the original files arrive.


## 2026-09-10 — Lab 5 completed locally with supplied original data

- User saved both assignment CSV files plus both small tutorial CSVs in the actual repository data/ directory. Assignment files contain 50 station rows / 50 route rows; five districts each contain 10 stations. No missing fields, duplicate rows/IDs/undirected pairs, invalid numerical values, self-links, or unknown endpoints were found. Raw data files were preserved byte-for-byte; tutorial files remain untouched.
- Viewed the actual rendered node-link diagram and district-ordered matrix, inspected station selections and route-cell tooltips, and cross-checked claims with independent Python CSV counts. Removed pending-data wording and completed all six observations in lab5/index.html. C1+C2+C3 total 257 whitespace-delimited English words including table headings and question labels, counting HTML cell boundaries as spaces.
- Observations: maximum degree is 3 with 14 tied stations (e.g. s7 and s23); South–East has 11 direct links, Central–North and East–West each 9; Transfer s23 branches while s50 is a leaf, Terminal s7 branches while s43/s46/s49 are isolated; top passengers s50=9850, s49=9677, s48=9504; longest routes s7–s31=16, s18–s34=15, s29–s50=14 minutes, all Express; both within-Central links are Shuttle but counts are too sparse for strong concentration claims. Network has one 45-node component and five isolated stations s42/s43/s46/s48/s49. Do not equate passenger volume, type label, or screen position with centrality.
- Reproduced one actual reduced-motion layout issue: s21 and s46 symbol bounds overlapped because simulation.tick(300) does not dispatch tick events, and bounds were applied only at the end. Factored keepNodesInBounds() in lab5/lab5.js and applied it after each manual simulation step. The regression fails before the patch and passes after it; ordinary animated mode also settles without overlapping station symbols.
- Real-data Playwright QA uses installed Chrome, actual local HTTP and live D3 CDN, without fixture interception. Passed 50 nodes / 50 routes / 2500 cells / 100 symmetric connected cells; all reported numerical claims; matrix sorting with unchanged relationships; station selection/highlighting; keyboard tooltips/Escape; real pointer drag and release; 390px mobile node/cell taps and local horizontal scrolling; standard/reduced-motion layout and pause/resume. All observed page/style/script/data/CDN responses were HTTP 200, with zero uncaught JS errors. Inspected real network/matrix screenshots and final answer table.
- Source CSV hashes unchanged: stations 81fab24ce5c88ebff8f3f289df8a3914cd5b40f67114da1c6a68188cff890455; routes 1be5728468089662740c6d1c8f80aac50897ad6fde45c61a147a6aadc647710c.
- QA script: Documents workspace tmp/lab5_actual_qa.cjs; actual rendered evidence, results, and screenshots under tmp/lab5-actual/. Earlier tmp/lab5-qa/ remains synthetic fixture evidence only. Updated output/notes/Lab5_讲解与作业思路.md with actual observations and current status.
- Local Lab 5 implementation is complete and verified. Remaining external steps: commit/push the required Lab 5 files, verify GitHub Pages, and submit the direct assignment URL. This turn did not commit, push, deploy, or submit. Preview remains http://127.0.0.1:8015/lab5/ from the actual Desktop repository. Existing untracked .DS_Store, PROJECT_CONTEXT.md, data/archive/, and user-provided tutorial data should not be swept into a future assignment commit without intent.


## 2026-09-13 — Lab 5 checklist, tutorial-method, and scope audit

- User asked to confirm all 16 submission checklist items, consistency with Tasks 1–16, and absence of unnecessary additions. This was a review request; assignment source, data, and tests were not modified. Read current lab5/index.html, lab5/lab5.js, scoped CSS, supplied Lab5.md, and existing QA script.
- Fresh node syntax and git whitespace checks passed. Re-ran tmp/lab5_actual_qa.cjs against real data and live D3 CDN: all existing checks passed, including 50/50 records, 2500/100 matrix cell counts, symmetric data, independent expected findings, drag/focus/touch interactions, normal/reduced-motion rendering, and 257-word design/question text. Separate real mouse hover on s23 showed s4/s22/s23/s24, its full tooltip and degree 3; leaving restored 50 nodes and hid the tooltip. Independently recounted core CSV observations with Python. Original CSV hashes match prior values.
- Checklist verdict: all 16 listed content/function requirements are covered in the current local page. The actual page loads the assignment CSVs, not the small practice files. Six answer rows contain answerability, selected view, encoding/interaction evidence, and actual observations. The page has concise design reasoning for both views.
- Method verdict must be qualified: same fundamental D3 workflow (Promise.all, numeric conversion, data/join, forceSimulation/link/charge/center/collide, tick, d3.drag fx/fy lifecycle, opacity highlights, tooltips, scaleBand matrix), but not identical tutorial implementation. Reasonable assignment-specific adaptation is path/symbol shapes for station_type and a linear area scale instead of the tutorial's circle radius sqrt scale; official D3 symbol documentation confirms size is area. Dash patterns are explicitly offered in Task 10, and district matrix ordering is encouraged in Part B/C.
- Unnecessary-for-this-small-assignment implementation substitutions: Task 13 some() became Map/Set neighbor lookup, and Task 16 nested forEach/find became flatMap plus a route Map. They are logically equivalent, but at 50 nodes the complexity/performance tradeoff is unnecessary for strict tutorial alignment. Recommend reverting these specific methods if user requests simplification.
- Optional UI/features previously added by assistant: station dropdown and persistent click selection/reset, pause/resume and reduced-motion manual simulation branch, matrix order switch, persistent inspector duplicating tooltip details, coordinated cross-view highlighting, extra positional forces. These were not required by the checklist. Do not claim the implementation contains no extras; acknowledge excess complexity under the user's preference. A minimal revision should keep fixed district ordering and all required encodings/drag/hover/tooltips/legends/findings while removing unnecessary controls and restoring tutorial-style searches. Basic validation/error messages and readable layout are useful safeguards, not grounds for blanket deletion.
- No source simplification, Git commit, push, or publication was performed in this audit. Any subsequent changes must preserve real data, required behavior, and correct current findings.


## 2026-09-13 — Approved Lab 5 simplification completed

- User approved the preceding recommendation to remove unnecessary additions while preserving every assignment requirement and tutorial methodology. Simplified actual repo lab5/lab5.js, lab5/index.html, and Lab 5 scoped CSS only; original data, earlier labs, design text, and six numerical findings were preserved.
- Removed station dropdown, persistent selection/reset, pause/resume with manual reduced-motion branch, matrix order switch, persistent inspector, and coordinated graph/matrix highlighting. Matrix is always ordered by district then numeric station ID. Basic keyboard/touch tooltip access remains with the same inspection handlers.
- Restored Task 13 links.some() checks in both directions and Task 16 nested nodes.forEach() with links.find(). Both views use the same links, whose source/target IDs become node objects after forceLink initialization; descriptive labels and matrix construction run after this conversion. Removed graph lookup Maps, neighbor Sets, flatMap matrix creation, route copies, and dual-format endpoint helper. Sets for validation were deliberately retained.
- Kept required district color, passenger symbol area, station shape, route width and dash style, force simulation, drag fx/fy lifecycle, node-link hover, independent matrix tooltips, legends, data validation, and error messages. Path/symbol with linear area is the necessary shape-encoding adaptation; gentle positional forces, collision, and bounds preserve legibility of this sparse network. JS decreased from 329 to 297 lines; no new product feature added.
- Fresh node --check and git diff --check passed. Browser QA in installed Chrome with actual local data and live D3 CDN verified 50 nodes / 50 routes / 2500 matrix cells / 100 symmetric connections; fixed district order; actual mouse neighbor highlighting and restore; keyboard node/route/cell tooltips; no cross-view highlight; real pointer drag and release; no overlapping settled node symbols; 390px touch tooltips inside viewport and no document overflow; all six answer facts; 257-word C1+C2+C3 text. All observed real-page resource responses were 200 or valid cache 304, with zero uncaught JS errors. Separate deliberately malformed CSV responses verified retained missing-ID, duplicate-ID, unknown-endpoint, and negative-time validation; original files were not changed. Both original CSV SHA256 values match previous checks.
- Inspected simplified node-link and matrix screenshots. New QA script is Documents workspace tmp/lab5_simplified_qa.cjs; results and screenshots are tmp/lab5-simplified/. Older QA artifacts remain historical evidence of the former UI, not tests of the current controls. Updated output/notes/Lab5_讲解与作业思路.md to describe current methods and remove stale control/manual-layout instructions.
- All 16 checklist content/function items remain fulfilled locally. No commit, push, GitHub Pages publication, or course submission performed. The local preview remains http://127.0.0.1:8015/lab5/; it is not a submission URL.


## 2026-09-13 — Matrix opacity legend covers all services

- User requested travel-time opacity examples for Metro, Express, and Shuttle, instead of Metro alone. Changed only the opacity legend loop in lab5/lab5.js to generate one example group per route type using the existing route colors and shared time/opacity scales. Each group illustrates the common 2–16 minute scale at opacity 0.35 and 1; these are scale examples, not claims about each service's observed range.
- Fresh node --check and git diff --check passed. Inspected the actual Chrome-rendered legend: all three service colors each have both correct time/opacity samples, no page errors; 390px viewport has no document overflow. Screenshot: Documents workspace tmp/lab5-simplified/matrix-legend-three-services.png.


## 2026-09-13 — Consolidated matrix opacity legend title

- User clarified that all three service opacity examples should share one title without “Metro example” or separate service headings. Updated lab5/lab5.js to render one “Travel time → cell opacity” group with two rows (2 and 16 minutes); each row contains the Metro, Express, and Shuttle colors side by side. Existing service color key identifies the colors.
- Fresh JS syntax and whitespace checks passed. Actual browser inspection confirmed a single opacity title, six correct swatches (three at 0.35 and three at 1), and no page errors. Screenshot: Documents workspace tmp/lab5-simplified/matrix-legend-unified.png.


## 2026-09-13 — Six findings changed to numbered paragraphs

- User approved replacing the six-question table with numbered short paragraphs using complete sentences. Changed lab5/index.html to an ordered list, preserving each question's answerability, selected visualization, visual encoding/interaction, and existing observations. Replaced the obsolete Lab 5 table CSS with a single list-indent rule; chart code and data were not modified.
- C1+C2+C3 now contain 261 whitespace-delimited English words including question labels (267 if counting six displayed list numbers), within the approximately 150–300 word total. Actual Chrome inspection confirmed six numbered paragraphs, no table or page errors, and readable wrapping at a 390px viewport without paragraph/document overflow. Inspected tmp/lab5-simplified/answers-paragraphs.png.
- git diff --check passed. Updated the teaching guide and existing local QA prose/count selectors to reflect paragraphs; the full interaction suite was not rerun for this text/layout-only change.


## 2026-09-13 — Separate question and answer lines

- User requested a line break between each question and its answer. Added one scoped CSS rule making .lab5-findings strong display as a block. All wording and numbering remain unchanged.
- git diff --check passed. Actual browser geometry confirmed that all six answers start below their question labels. Screenshot: Documents workspace tmp/lab5-simplified/answers-line-breaks.png.
